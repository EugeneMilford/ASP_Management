@page
@model OfficeManagement.Pages.OfficeProjects.IndexModel

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Projects</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="/Index">Home</a></li>
                        <li class="breadcrumb-item active">Projects</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>
     
    <a asp-page="Create" class="btn btn-secondary">Add New Project</a>

    <br />
    <br />

        <div class="card-body">
            <table class="table table-striped projects">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Project[0].ProjectName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Project[0].Description)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Project[0].CreatedDate)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Project[0].ProjectUser)
                        </th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model.Project) {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.ProjectName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreatedDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ProjectUser)
                        </td>
                        <td>
                            <a asp-page="./Edit" asp-route-id="@item.ProjectId">
                                <i class="fas fa-pencil-alt"></i>
                                    Edit
                            </a> |
                            <a asp-page="./Details" asp-route-id="@item.ProjectId">
                                <i class="fas fa-folder"></i>
                                    Details
                            </a> |
                            <a asp-page="./Delete" asp-route-id="@item.ProjectId">
                                <i class="fas fa-trash"></i>
                                    Delete
                            </a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>






